{
  "name": "riverside-product-suite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build:shelves && vite",
    "build:shelves": "npm --prefix apps/shelves install && npm --prefix apps/shelves run build",
    "build": "npm run build:shelves && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
